export interface ITheme {
  palette: {
    primary: string
    secondary: string
    gold: string
    silver: string
    bronze: string
    error: string
  }
}

export const theme: ITheme = {
  palette: {
    primary: '#ffffff',
    secondary: '#bb1e79',
    gold: '#ffd54c',
    silver: '#969696',
    bronze: '#b76717',
    error: '#ff0000',
  },
}
